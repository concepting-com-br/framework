<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE artifacts>

<artifacts>
	<artifact packageName="br.com.concepting.framework.controller.form.util.ActionFormUtil.getActionFormPackageByModel(#{clazz})"
	          name="br.com.concepting.framework.controller.form.util.ActionFormUtil.getActionFormNameByModel(#{clazz})">
		package @{packageName};
		<set var="serialVersionUID" value="br.com.concepting.framework.model.util.ModelUtil.generateSerialVersionUID()"/>

		import br.com.concepting.framework.controller.form.annotations.ActionForm;
		import br.com.concepting.framework.controller.form.BaseActionForm;

		import #{clazz.name};
		<set var="actionFormName" value="br.com.concepting.framework.controller.form.util.ActionFormUtil.getActionFormIdByModel(#{clazz})"/>
		<set var="actionFormUrl" value="br.com.concepting.framework.model.util.ModelUtil.getUrlByModel(#{clazz})"/>

		/**
		 * Class that defines the form of the #{clazz} UI.
		 *
		 * @author @{user.name}
		 * @version @{build.version}
		 * @since @{now}
		 */
		@ActionForm(name="@{actionFormName}", action="@{actionFormUrl}")
		@SuppressWarnings("javadoc")
		public class @{name} extends BaseActionForm&lt&lt;#{clazz.simpleName}&gt&gt;{
			private static final long serialVersionUID = @{serialVersionUID}L;
		}
	</artifact>
</artifacts>